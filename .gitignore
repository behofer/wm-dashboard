# dependencies (this project is zero-dependency, but just in case)
node_modules/

# secrets & environment — NEVER commit the real API key
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/

# local Claude Code tooling (personal settings)
.claude/

# scratch / harness files
_*.mjs
_*.js
